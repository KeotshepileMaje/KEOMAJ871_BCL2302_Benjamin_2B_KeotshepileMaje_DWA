import { State} from './store.js'
import {Action} from './actions.js'

/**
 * @param {State} state
 * @param {Action} action
 * @return {State}
 */

const reducer = (state, action) => {
    return state
}